import {Input} from "@/components/ui";

const SearchInput = () => (
    <>
        <Input
            placeholder="Enter name of a whiteboard"
            className="flex-grow"
        />
    </>
);

export default SearchInput;